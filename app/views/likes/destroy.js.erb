var like_section = document.getElementById('like_section_<%= @post.id %>');
var like_section_modal = document.getElementById('like_section_modal');
like_section.innerHTML = '<%= escape_javascript(render partial: "shared/like", locals: { post: @post }) %>';
like_section_modal.innerHTML = '<%= escape_javascript(render partial: "shared/like", locals: { post: @post }) %>';